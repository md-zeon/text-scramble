import type { GenerateFramesOptions } from "../types";

/**
 * Generates every frame required for a scramble animation.
 */
export function generateFrames(options: GenerateFramesOptions): string[] {
  return [];
}
